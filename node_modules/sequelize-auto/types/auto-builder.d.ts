import { QueryInterface, Sequelize } from "sequelize";
import { DialectOptions } from "./dialects/dialect-options";
import { TableData } from "./types";
export declare class AutoBuilder {
    sequelize: Sequelize;
    queryInterface: QueryInterface;
    dialect: DialectOptions;
    includeTables?: string[];
    skipTables?: string[];
    schema?: string;
    views: boolean;
    tableData: TableData;
    constructor(sequelize: Sequelize, tables: string[] | undefined, skipTables: string[] | undefined, schema: string | undefined, views: boolean | undefined);
    build(): Promise<TableData>;
    private processTables;
    private mapForeignKeys;
    private mapTable;
    private executeQuery;
}
